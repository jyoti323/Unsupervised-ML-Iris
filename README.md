# Unsupervised-ML-Iris
This project demonstrates **unsupervised machine learning** using the famous **Iris dataset**.   The goal is to group flowers into clusters based on their features using the **K-Means clustering algorithm**.
